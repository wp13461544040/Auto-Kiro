# KiroX 功能更新说明

## 1. 批量导入代理功能

### 功能描述
- 支持一行一个代理地址的批量导入
- 支持多种代理格式：
  - 完整URL格式：`http://proxy.com:8080`、`socks5://user:pass@proxy.com:1080`
  - 简写格式：`host:port:user:pass`、`host:port`、`user:pass@host:port`

### 使用方法
1. 进入"设置"页面
2. 找到"代理"设置项
3. 点击"批量导入"按钮
4. 在弹出的对话框中，每行输入一个代理地址
5. 点击"导入"按钮完成导入

### 技术实现
- **前端**：
  - 新增 `showBatchImportModal()` 和 `batchImportProxies()` 函数
  - 添加批量导入模态框UI（`proxy-batch-import-modal`）
  - 支持多行文本输入，自动过滤空行
  
- **后端**：
  - 复用现有的 `AddProxyEntry` API
  - 自动归一化代理地址格式
  - 返回成功和失败统计

## 2. 注册目标模式

### 功能描述
- 新增"目标模式"选项，勾选后注册数量变为目标数量
- 意外中断或失败后，自动开启新一轮注册
- 持续运行直到成功注册数达到目标数量

### 使用方法
1. 进入"注册"页面
2. 在"注册数量"输入框下方勾选"目标模式"
3. 设置目标数量（例如：50）
4. 启动任务后：
   - 如果第一轮注册了100个，成功30个，失败70个
   - 系统会自动等待3秒后开启第二轮，继续注册剩余的20个（50-30=20）
   - 直到成功数达到50个为止

### 核心逻辑
- **不再是"注册总数"，而是"成功目标数"**
- 每轮结束后检查：`成功数 >= 目标数`？
  - 是：任务完成
  - 否：等待3秒，自动开启新一轮，剩余目标 = 目标数 - 当前成功数
- 支持手动停止：用户点击停止按钮后，不会再自动重启

### 技术实现

#### 前端修改
1. **HTML** (`frontend/index.html`):
   - 添加"目标模式"复选框和提示tooltip
   - 复选框ID：`cfg-target-mode`

2. **JavaScript** (`frontend/js/app.js`):
   - `getFormConfig()` 函数增加 `targetMode` 字段
   - 将目标模式状态传递给后端

3. **CSS** (`frontend/css/components.css`):
   - 新增tooltip样式，用于显示功能说明

#### 后端修改
1. **任务请求结构** (`internal/task/coordinator.go`):
   ```go
   type StartTaskRequest struct {
       // ...现有字段
       TargetMode bool `json:"targetMode"` // 新增
   }
   ```

2. **任务执行逻辑** (`runBatch` 函数):
   - 在任务完成后检查是否启用目标模式
   - 如果成功数未达标且未被手动停止，自动开启新一轮
   - 新一轮的数量 = 目标数 - 当前成功数
   - 累计所有轮次的成功数，只重置 `completed` 和 `failed`
   - 每轮之间等待3秒，避免频繁重启

3. **日志输出**:
   - 目标模式下显示："本轮完成 — 总计: X, 成功: Y, 失败: Z (目标: N)"
   - 非目标模式显示："任务完成 — 总计: X, 成功: Y, 失败: Z"

### 使用场景
- **稳定性保障**：网络抖动、临时故障不会导致任务中断
- **资源优化**：不需要手动重启，节省人工监控成本
- **目标导向**：更关注最终成功数，而非尝试次数

### 注意事项
1. 目标模式下，系统会自动重试，建议设置合理的目标数量
2. 可随时点击"停止"按钮终止自动重试
3. 每轮统计独立显示，便于分析成功率变化趋势
4. 熔断机制仍然生效（如遇到IP被封等严重错误会立即停止）

## 构建和运行

编译项目：
```bash
cd c:\Users\Administrator\Desktop\kiroX
go build -o kiroX.exe
```

运行程序：
```bash
.\kiroX.exe
```

## 文件修改清单

### 前端文件
- `frontend/index.html` - 添加批量导入模态框和目标模式UI
- `frontend/js/proxy_pool.js` - 添加批量导入功能
- `frontend/js/app.js` - 添加targetMode配置项
- `frontend/css/components.css` - 添加tooltip样式

### 后端文件
- `internal/task/coordinator.go` - 添加目标模式逻辑和自动重试机制

## 测试建议

1. **批量导入代理测试**：
   - 准备包含多种格式的代理列表
   - 测试空行处理
   - 测试重复代理检测
   - 测试错误格式提示

2. **目标模式测试**：
   - 设置目标数量为10，实际可用账号5个
   - 验证第一轮完成后是否自动开启第二轮
   - 测试手动停止是否能正常终止自动重试
   - 验证累计成功数是否正确
